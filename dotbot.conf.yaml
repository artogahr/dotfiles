- clean: ['~']

- defaults:
    link:
      relink: true
      force: true

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/ranger: ranger
    ~/.config/hypr: hypr
